import { Injectable } from '@angular/core';
import {HttpClient} from '@angular/common/http';

@Injectable({
  providedIn: 'root'
})
export class QuizService {
  // Properties
  // readonly rootUrl = 'http://localhost:4200/home';
  // // public quiz: any[]={seconds: 'seconds', timer: timer, qnsProgress: ''}
  // private var;
  // seconds: number;
  // timer: number;
  // qnProgress;


  // Helper Methods
  constructor(private http: HttpClient) { }

  // HTTP Methods
}
